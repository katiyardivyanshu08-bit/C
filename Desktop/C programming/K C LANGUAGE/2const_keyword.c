// Variables can be defined as constants means they can't be changed.
// To define a const., use the const keyword before the DT of var.
// Constants must be assigned a value while declared.
#include <stdio.h>
int main() {
    const int num=62;
    printf("%d",num);
    // const int n;     WRONG METHOD
    // n=62; 
}