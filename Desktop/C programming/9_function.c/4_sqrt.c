#include <stdio.h>
#include <math.h>
int main()
{ system("cls");
    int i,n;
    // printf("Enter a number : ");scanf("%d",&n);
    // printf("The square root id : ");
    // float root=sqrt(n);
    // printf("%.4f",root);
    int power=pow(2,3);
    printf("%d",power);
    return 0;
}
